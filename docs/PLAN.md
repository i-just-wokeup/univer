# PLAN

## 현재 진행 단계
**스토어 제출 단계**
- 🤖 **Google Play** — 앱 생성·앱 콘텐츠 선언·데이터 안전 폼·프로덕션 AAB 빌드 완료. 남은 것: 스토어 등록정보(피처 그래픽 1024×500 + 스크린샷), 비공개 테스트 출시, **테스터 12명+ 모집·수락 → 14일 연속**(시계는 12번째 수락 시점부터 시작)
- 🍎 **Apple** — 메타데이터·개인정보 라벨·심사정보 완료, 프로덕션 빌드 진행. 남은 것: iOS 스크린샷, `eas submit`, 심사 제출. **12명·14일 요건이 없어 더 빠른 출시 경로**
- 출시 후: 좋아요 목록, 알림 설정, 스토리 하이라이트, 인스타 임포트 연구, 웹 리브랜딩

(이전 단계: 웹 MVP → 앱 전환 → 색 토큰화·다크모드 → 출시 준비)

---

## 완료된 작업
- [x] 앱 게시물 미디어 선택 목록 저장 최신순 정렬 — 사진·영상 및 앨범별 그리드를 촬영 시각 대신 기기 저장·수정 시각 기준으로 정렬해 외부에서 받아 저장한 미디어를 상단에 노출
- [x] 앱 홈피드 최신 게시물 백그라운드 갱신 — 캐시 생성 시각의 상호작용 변경 연장을 차단하고 캐시 즉시 표시 후 재검증, 홈 재진입·탭 재선택 시 무스피너 첫 페이지 갱신 적용
- [x] 프로덕션 빌드 환경변수 누락 수습 — `.env.local`이 gitignore되어 EAS에 업로드되지 않던 문제. EXPO_PUBLIC 5개를 EAS 환경변수(production)로 등록, preview 프로필에도 `environment` 추가
- [x] 앱 릴스 processing·failed 영상 제외 — 홈의 본인 업로드 폴링은 유지하고 릴스는 본인 포함 ready 영상만 노출; 스토리는 기존 작성자 처리 상태/타인 ready 노출 정책 유지
- [x] 웹 가이드라인에 아동 안전 표준(CSAE) 섹션 추가 + 초안 배너 제거·문의 연락처 확정 — Google Play 소셜 카테고리 필수 공개 문서
- [x] 앱 하단 프로필 탭 Android 활성 링 SVG 교체 — border·배경 채움 방식의 사각형 렌더링 실패를 정정하고 SVG 원으로 플랫폼 차이 회피
- [x] 앱 Sentry 소스맵 업로드 설정 연결 — `app.json` 플러그인에 org/project 지정, `eas.json` production에 `environment` 추가, 토큰은 EAS Secret(production)으로만 등록
- [x] 앱 스토리 카메라 촬영 결과 9:16 정규화 — 촬영 이미지를 세로 방향으로 보정한 뒤 중앙 9:16으로 크롭해 스토리 캔버스 레터박스 방지
- [x] 앱 스토리 작성 공개범위·게시 버튼 정리 — 크루공개 선택을 제거하고 즉시 게시 동작을 `게시하기` 텍스트로 명확화
- [x] 앱 게시물 상세 공유 기능 연결 — 모든 상세 진입 경로에서 DM·크루·외부 링크 공유와 권한 있는 본인 글의 스토리 리셰어 지원
- [x] 앱 댓글 시트 키보드 여백 수정 — 시트 전체 리사이즈를 제거하고 입력 영역만 keyboard-controller로 이동하며 키보드 표시 중 safe-area 추가 여백 제외
- [x] 웹·앱 검색 해시태그 안내 제거 — 실제 닉네임 검색 범위에 맞춰 placeholder와 빈 검색 안내 문구 정리
- [x] 웹 데스크톱 사이드바·설정 화면 앱 디자인 정렬 — `/settings` 링크와 활성 표시, 중립 배경·절제된 활성 메뉴·무그림자 카드·데스크톱 반응형 폭을 적용하고 모바일 하단 탭 구성은 유지
- [x] 앱 온보딩 키보드 회피·공식 계정 프로필 편집 정리 — 포커스 입력 자동 스크롤과 키보드 상태의 하단 액션 접근을 보장하고 공식 계정의 학생용 학과·공개 토글 숨김
- [x] 앱 이메일 계정 비밀번호 변경 — 이메일·비밀번호 계정에만 설정 진입점을 노출하고 현재 비밀번호 재인증 후 8자 이상 영문·숫자 조합의 새 비밀번호로 변경
- [x] 웹 공식 계정 관리자 화면 — `/admin/officials`에서 학생회·동아리 계정 발급, 임시 비밀번호 전달, 공식 계정 목록·유형 변경·해제를 관리하도록 Edge Function/RPC 계약 연결
- [x] 앱 최근 업로드 홈피드 10분 유지 — 사진·영상·글 업로드를 시간창 목록에서 도출해 새로고침 후에도 최신순 상단 유지하고, 본인 글 캐시 제외로 만료 후 재노출 방지
- [x] 앱 Supabase 토큰 자동 갱신 수명주기 연결 — SessionProvider에서 foreground 시작·background 중지·언마운트 정리를 한 번만 등록해 장시간 실행 후 만료 JWT 401 방지
- [x] 앱 업로드 직후 본인 홈피드 임시 표시 — 생성된 게시물 ID를 feature 채널로 전달해 기존 hydration 결과를 홈 맨 앞에 추가하고 영상 processing 폴링·완료 토스트를 연결
- [x] 앱 인코딩 중·실패 영상 타인 노출 차단 — 공용 피드 hydration과 스토리 목록 필터에서 ready가 아닌 영상은 작성자 본인에게만 노출
- [x] 앱 하단 프로필 탭 아바타 표시 — 세션의 기존 프로필 조회 결과를 재사용해 내 아바타·닉네임을 탭바에 노출하고 프로필 저장·로그아웃·계정 전환에 맞춰 즉시 갱신
- [x] 웹 공개 고객지원 페이지 추가 — 고객지원 경로에 문의 이메일·FAQ·정책 및 계정 삭제 링크를 제공하고 비로그인 공개 경로로 등록
- [x] 웹 이용약관 UGC 정책 추가 — 부적절 콘텐츠 무관용, 앱 내 신고 후 24시간 이내 검토, 콘텐츠 삭제·차단·이용 정지 및 운영 문의 이메일을 명시해 Apple 1.2·Google UGC 정책 대응 문구 마련
- [x] 승격 신청 자격 상향 — 프로 계정 신청 자격을 게시물 10개 + 최근 30일 5개로 상향(기존 30일 3개). `request_promotion` RPC(라이브 적용)·앱 `PromotionCard` 문구 반영, 마이그레이션 `20260814090000` 파일화(커밋 대기)
- [x] 웹 소비자 피드 영상 렌더 수정 — `PostCard`가 Cloudflare 영상을 `next/image`에 넘겨 홈피드가 크래시하던 문제를 `PostMedia` provider 복구 + 영상 iframe/`<video>` 분기로 해결(웹 tsc 통과, 커밋 대기)
- [x] 승격 신청 앱 3단계 — 일반 계정 인사이트 개요 하단에 프로 계정 신청 카드와 최신 신청 상태·7일 재신청 쿨다운을 연결하고, 자격 검증은 `request_promotion` 서버 계약에 위임. 승인·거절 인앱/푸시 알림 문구와 `/insights` 이동까지 반영
- [x] 승격 심사 게시물 상세 — 게시물 검토 모달에 미디어·조회/도달/참여·영상 시청 지표와 관리자 전용 댓글 목록을 연결하고, 두 SECURITY DEFINER RPC 내부 관리자 검증·ACL을 적용
- [x] 승격 신청 웹 관리자 2단계 — `/admin/promotions`에 대기 목록·활동 성적표·신청자 게시물 그리드·Cloudflare Stream/레거시 영상/이미지 라이트박스를 추가하고 승인·거절 RPC 처리 후 목록 갱신·토스트까지 연결
- [x] 승격 신청 DB 1단계 — `promotion_requests` 본인 조회 RLS·pending 부분 유니크를 추가하고, 자격/중복/7일 쿨다운 신청 RPC와 관리자 성적표·승인·거절 RPC, 승인/거절 인앱·푸시 알림 타입을 연결. 일반 사용자의 직접 `is_promoted` 변경은 계속 차단
- [x] RPC 보안 감사 배치 2 — 검색·실명·연결 상태에 양방향 차단 서버 필터를 추가하고, `recount_*` 호출 권한 검증·누락 함수 8개 로컬 백필·지표/배지 RPC ACL 축소·정렬 RPC 인자 상한을 적용
- [x] 고위험 RPC 보안 감사 배치 1 — `record_metric`/`record_reel_watch` owner를 서버 계산으로 전환하고, 크루 신청·수락에 활성/탈퇴·같은 학교·양방향 차단 검증과 무방향 유니크·역방향 pending 자동 수락을 적용
- [x] 서비스 방향 확정 (대학생 실명 SNS UniVerse)
- [x] 기술 스택 확정
- [x] 화면 구조 및 라우팅 확정
- [x] 메인 레이아웃 껍데기 구현 (`Header`, `BottomTabBar`, `SideBar`, `app/(main)`)
- [x] 학교 이메일 매직링크 로그인 기본 흐름 구현
- [x] 이메일 + 비밀번호 로그인 / 회원가입 / 온보딩 구현
- [x] 게시물 작성 페이지 및 이미지 업로드 저장 로직 구현
- [x] 피드 조회 API (`getFeed`) 구현
- [x] 피드 UI 1차 구현 (`PostCard`, `FeedList`, `app/(main)/page.tsx` 연결)
- [x] 웹 사이드바 고정 및 피드 레이아웃 1차 보정
- [x] DB 스키마 설계 완료 (24개 테이블)
- [x] 프로젝트 폴더 생성 (`C:\dev\univer`)
- [x] Next.js 프로젝트 생성
- [x] GitHub 레포 생성 및 초기 커밋
- [x] 문서 세팅 (AGENTS.md, CLAUDE.md, docs/)
- [x] Supabase universe 프로젝트 생성
- [x] 환경변수 설정 (.env.local)
- [x] Notion 문서 세팅
- [x] 게시물 수정/삭제 구현
- [x] 토스트 메시지 구현 및 피드 연결
- [x] 스토리 업로드/스토리바/뷰어 구현
- [x] ConfirmDialog 공용 컴포넌트 구현
- [x] stories RLS soft delete 호환 정책 수정
- [x] database.types.ts users real_name 컬럼 반영
- [x] 프로필 조회 API 1차 구현 (`src/features/profile/api.ts`)
- [x] 프로필 페이지 1차 구현 (`/profile/[nickname]`)
- [x] 프로필 편집 페이지 구현 (`/profile/edit`)
- [x] 프로필 수정 로직 분리 (`src/features/profile/mutations.ts`)
- [x] 로그아웃 API 추가 (`signOut()`)
- [x] 전체 button 태그 cursor-pointer 전역 적용
- [x] 설정 페이지 (`/settings`) — 로그아웃 포함
- [x] 프로필 편집에서 로그아웃 버튼 제거
- [x] 프로필 페이지에 설정 아이콘(톱니바퀴) 추가
- [x] 자체 헤더 화면용 `(sub)` route group 분리
- [x] 게시물 상세 UI 컴포넌트 구현 (`PostDetail`)
- [x] 게시물 상세 직접 접근 페이지 구현 (`/posts/[postId]`)
- [x] 게시물 상세 웹 인터셉트 모달 구현 (`@modal/(.)posts/[postId]`)
- [x] 게시물 상세 모달 (썸네일 클릭 시)
- [x] 프로필 게시물 썸네일 상세 이동 연결
- [x] Avatar 공용 컴포넌트 구현 및 피드/댓글/스토리/프로필 적용
- [x] UserInfo 공용 컴포넌트 (프로필 링크)
- [x] NavItems 클라이언트 컴포넌트 구현 및 현재 경로 기반 활성 상태 처리
- [x] 모달 내 UserInfo 링크 클릭 시 모달 닫기 처리
- [x] 사이드바 프로필 활성화 조건 수정
- [x] 스토리바 내 스토리 아바타 및 그라데이션 링 UI 적용
- [x] 게시물 작성 페이지 `/write` 경로 추가 및 작성/수정 링크 변경
- [x] 메인 피드 컬럼 폭 `max-w-[470px]` 복구
- [x] 게시물 미디어 테이블 `post_images` → `post_media` 전환 반영
- [x] `reports` 테이블 신규 생성
- [x] 피드/스토리 신고 API 연결
- [x] 유저 앱 신고 버튼 기능
- [x] 알림 API 및 웹/모바일 알림 UI 구현
- [x] 알림 시스템
- [x] 관리자 페이지 1차 구현 (`/admin`, `/admin/reports`, `/admin/users`)
- [x] 관리자 페이지 구현
- [x] 회원가입 플로우 재설계
- [x] 유저 검색
- [x] 친구(크루) 시스템 구현
- [x] React 19/Next 16 hook lint 에러 수정 및 build 검증
- [x] 크루 목록 및 요청 관리 페이지 구현 (`/profile/connections`)
- [x] 스토리 목록 정렬 개선 (내 스토리 → 크루 → 같은 학교)
- [x] 크로스 유저 스토리 네비게이션 구현
- [x] 스토리 뷰어 3열 그리드 레이아웃 보정
- [x] 기존 `/posts/write` 라우트 삭제
- [x] 채팅 (1:1 DM) 1차 구현
- [x] 채팅 메시지 페이지네이션 (최신 50개 초기 로드, 스크롤 위로 이전 메시지 추가 로드)
- [x] 채팅 메시지 낙관적 전송 처리 (임시 메시지 추가/교체/실패 제거, Realtime 중복 방지)
- [x] 채팅 전송 후 UX 보정 (입력창 포커스 유지, 전송 후 하단 스크롤)
- [x] 채팅 연속 전송 UX 개선 (전송 중 입력창 잠금 제거)
- [x] 채팅 수신 메시지 자동 스크롤 (상대방 메시지 수신 시 하단 자동 이동)
- [x] 앱 게시물 인앱 DM 공유 1차 (피드 공유 → 대화 선택 → post 메시지 카드 → 게시물 상세 이동)
- [x] 앱 게시물 공유 바텀시트 2단 detent 개선 (55% 반열림 → 92% 확장 → 드래그 닫기)
- [x] 앱 게시물 공유 시트 구조 분리 리팩토링 (드래그 훅/공유 대상 리스트/외부 공유 영역 분리)
- [x] 앱 릴스 아이템 구조 분리 리팩토링 (영상 플레이어 훅/액션/푸터/더보기 메뉴 분리)
- [x] 앱 피드 카드 구조 분리 리팩토링 (헤더/액션/본문/더보기 메뉴 분리)
- [x] 앱 피드 미디어 캐러셀 구조 분리 리팩토링 (영상/사진 분기와 사진 캐러셀 분리)
- [x] 앱 채팅방 화면 구조 분리 리팩토링 (요청 배너/메시지 리스트/차단 메뉴 분리)
- [x] 앱 피드 API 역할별 분리 리팩토링 (조회/작성·삭제/상호작용/업로드/영상상태/hydration 분리)
- [x] 앱 댓글 시트 구조 분리 리팩토링 (목록/스레드/입력바/액션메뉴/드래그 훅 분리)
- [x] 앱 홈 화면 구조 분리 리팩토링 (피드 리스트/시트/피드백/상태/네비게이션/영상 폴링 분리)
- [x] 앱 홈 피드 훅 책임 분리 리팩토링 (페이지 로딩/피드 액션/복귀 동기화 분리)
- [x] 앱 채팅 API 역할별 분리 리팩토링 (대화/메시지/공유/안읽음/타입 분리)
- [x] 앱 스토리 API 역할별 분리 리팩토링 (업로드/생성·삭제/조회/상호작용/타입 분리)
- [x] 앱 알림 API 역할별 분리 리팩토링 (조회/읽음처리/메타보강/관련데이터/화면모델 분리)
- [x] 앱 내 활동 API 역할별 분리 리팩토링 (스토리/게시물/즐겨찾기/공통 hydration/타입 분리)
- [x] 앱 프로필 API 역할별 분리 리팩토링 (조회/크루 상태·목록/즐겨찾기/내부 유틸 분리)
- [x] 앱 프로필 편집 화면 구조 분리 리팩토링 (아바타/닉네임/소개/링크/저장 버튼 UI 분리)
- [x] 앱 프로필 편집 Samsung Pass 오탐 완화 (닉네임/소개 입력 모달 분리 + 소개 입력 키보드 타입 조정)
- [x] 앱 프로필 메인 화면 구조 분리 리팩토링 (헤더/본문/더보기 메뉴/게시물 그리드 분리)
- [x] 앱 게시물 작성 화면 구조 분리 리팩토링 (헤더/미디어/영상미리보기/본문/설정 분리)
- [x] 앱 새 게시물 사진 선택 화면 1차 (MediaLibrary 최신순 3열 그리드, 다중 선택·비율 미리보기, 기존 작성 폼 연결)
- [x] 앱 새 게시물 사진 선택 UX 보정 (Android EXIF 위치 권한 오류 회피, 미리보기 하단 레이어·갤러리 BottomSheet 분리, 일반 선택 후 그리드 offset 유지, 패널 제어 훅·그리드 목록·사진 셀·툴바·상태 UI 책임 분리)
- [x] 앱 새 게시물 사진 앨범 선택 (앨범 커버 가로 목록·모두 보기 2열 그리드, MediaLibrary 어댑터 분리, iOS 스마트 앨범·Android 폴더, 앨범별 페이지네이션·빠른 전환 요청 무효화, 폼 `+` 재진입·삭제 동기화·취소 복원)
- [x] 앱 내 활동 스토리 미리보기 시트 구조 분리 리팩토링 (미디어/메타/조회자 목록/조회자 row 분리)
- [x] 앱 세션 컨텍스트 조회 캐시 ② 적용 (university_id 세션 캐시, 차단목록 30초 TTL/in-flight dedupe, 차단/로그아웃 무효화)
- [x] 앱 게시물 조회 왕복 최적화 ③ 적용 (피드/릴스/상세 posts 쿼리에 작성자·미디어 PostgREST 임베딩 적용)
- [x] 앱 홈 피드 재진입 즉시표시 캐시 ⑥-1 적용 (90초 TTL 메모리 캐시, userId guard, focus 재조회 스킵)
- [x] 앱 탐색/프로필 재진입 즉시표시 캐시 ⑥-2 적용 (탐색 90초 TTL, 프로필 닉네임별 60초 TTL + 뷰어 userId guard)
- [x] 앱 체감 로딩 개선 1차 (홈/탐색/프로필 스켈레톤, 하단 로딩 간소화, 이미지 prefetch/recyclingKey 확대)
- [x] 앱 콜드스타트 온보딩 게이트 단축 (프로필 조회 1회 통합, auth.getSession 전환, userId별 온보딩 완료 로컬 플래그)
- [x] 앱 스토리 공용 영상 뷰 release 경합 방어 (비활성 시 VideoView 분리, play/pause 예외 방어)
- [x] 앱 스토리 닫기 잔상 완화 (닫기 전 미디어 detach 후 기본 pop 전환 유지)
- [x] 앱 스토리 전환/카메라 잔상 완화 (홈 storyGroups 전달, 카메라 준비 전 커버, 영상 textureView/poster 적용)
- [x] 앱 스토리 뷰어 전역 세션 캐시 제거
- [x] 앱 스토리 뷰어 시작 유저/이전탭 상태 꼬임 수정
- [x] 앱 홈 피드 리렌더 최적화 1차 (콜백 안정화, FeedPostCard memo, FlatList 보수 튜닝)
- [x] 앱 좋아요 낙관적 업데이트 적용 (홈/릴스/상세 하트 즉시 반영 + 실패 시 롤백)
- [x] 앱 화면 전환 애니메이션 명시 (기본 slide_from_right, 릴스/스토리/작성 slide_from_bottom, 로그인/온보딩 none)
- [x] 앱 화면 전환 체감 속도 조정 (기본 simple_push, 릴스 none)
- [x] 앱 스토리/릴스 전환 옵션 재조정 (스토리 fade, 릴스 simple_push)
- [x] 앱 Android 엣지투엣지 opt-out 보강 (theme opt-out config plugin, 릴스/스토리 시스템바 예외 중앙화)
- [x] 앱 Android 엣지투엣지 ON 2a 전환 (opt-out 제거, 시스템바 tint 중심 축소, EAS 빌드 후 깨지는 화면 목록화 예정)
- [x] 앱 Android 엣지투엣지 2b 시스템바 배경/inset 정리 (일반 화면 네비바 흰 배경, 릴스/스토리 검정 유지, 주요 스크롤 화면 하단 safe-area 보정)
- [x] 앱 Android 엣지투엣지 2b 보정 (하단 네비바 배경 공용 레이어 zIndex/elevation 보강)
- [x] 앱 Android 엣지투엣지 하단 네비바 흰색 방향 검증 1차 (인사이트 화면 내부 safe-area 배경 직접 렌더)
- [x] 앱 프로필 실명/학과 공개 여부 토글 Phase 1 (DB 공개 플래그/RPC 마스킹, 앱 표시 경로 학과 마스킹, 프로필 편집 스위치)
- [x] 앱 홈피드 순서 함수 연동 (get_feed_post_ids 기반 band/rank 페이지네이션, post_impressions 노출 기록, 모두 열람 마커)
- [x] 앱 홈피드 band 1 핫스코어 정렬 (기본점수 1+좋아요+댓글 가중치, 5일 반감기; rank 동률 방지, band 0/2·커서 유지)
- [ ] 다학교·크로스스쿨 피드 확장 시 학교 내부 좋아요·댓글 카운트로 핫스코어 분리
- [x] 앱 릴스 순서 함수 연동 (get_reel_post_ids 기반 세션 셔플, 안 본 영상 우선, 무한 루프)
- [x] 앱 홈 탭 재탭 UX 적용 (스크롤 내려간 상태면 맨 위로, 최상단이면 새로고침)
- [x] 앱 색상 의미 토큰화 1단계 (`COLOR_TOKENS.md` 기준 신규 토큰 38개 추가, raw hex/rgba 치환, 제외 목록 유지)
- [x] 앱 다크모드 Phase 0 배선 (`lightColors`/`darkColors`, ThemeProvider/useTheme/useThemedStyles, 시스템 테마 자동 구독, 기존 화면 라이트 하위호환)
- [x] 앱 다크모드 Phase 1 홈 탭 전환 (공통 프리미티브, 홈 피드 카드·스토리바, 댓글·공유 시트 테마 대응)
- [x] 앱 다크모드 Phase 2 프로필 탭 전환 (프로필·편집·크루 관리, 하단바 이음새 제거, 미전환 화면 라이트 기본값 복구)
- [x] 앱 다크모드 Phase 3 채팅 전환 (말풍선 대비 onAccent) + G2 검색/탐색/알림 + G3 작성/게시물상세 + G4 설정/활동/인사이트/인증 — **일반 화면 다크 전환 완료** (미디어=릴스/스토리는 fixed라 대상 아님)
- [x] 앱 다크 팔레트 튜닝 (글씨 메타 그레이 #E4E6EB/#B0B3B8/#8A8D91, 딥다크 배경 #000·카드/내비 표면 #0F1011, 프로필 카드 피드와 통일, 상태바 다크 대응, 헤더 아이콘 원형 제거)
- [x] 앱 장식 아웃라인 테두리 제거 (카드·버튼·칩·말풍선; 입력필드·구분선·스토리 링은 유지)
- [x] 앱 unip 워드마크 로고 적용 (Logo 컴포넌트, KREW 텍스트 5곳 교체)
- [x] 앱 인증 배지 1~2차 (공식/승격 유저 ID 5분 캐시·요청 dedupe, 세션 Provider, 홈·댓글·프로필·크루·채팅·공유·릴스·검색 닉네임 배지)
- [x] 앱 타이포 토큰화 1차 (실사용 fontSize 14종·fontWeight 6종 스케일 정의, src 하드코딩 435건 1:1 무변화 치환)
- [x] 앱 일반 화면 하단 네비바 배경 방향 재정리 (탭 화면 흰색 유지, 일반 화면은 콘텐츠 배경색 연장)
- [x] 앱 Android 네비바 contrast scrim 비활성화 (androidNavigationBar.enforceContrast=false, 실패한 JS 하단 오버레이 정리)
- [x] 앱 일반 화면 하단 네비바 흰색 공용 컨테이너 적용 (ScreenContainer로 흰 루트/연보라 콘텐츠 배경 분리)
- [x] 앱 햅틱 피드백 1차 도입 (더블탭 좋아요/당겨서 새로고침)
- [x] 앱 채팅 키보드 3a 적용 (react-native-keyboard-controller 설치, 채팅방 입력창 KeyboardStickyView 전환)
- [x] 보안 후속: 탈퇴 계정 영구삭제 보존기간 하한 강제 (Edge Function 입력검증 + DB 함수 30일 미만 거부)
- [x] 게시물 외부 공유 Phase 2 (public 게시물만 `/p/[postId]` 웹 미리보기 + 앱 OS 공유)
- [x] 앱 게시물 공유 시트 외부 공유 푸터 고정 (55%/92% detent와 독립된 화면 하단 위치, 대상 목록만 스크롤)
- [x] 채팅방 fullscreen route group 분리 (하단 탭바/사이드바 제거, `h-dvh` 전용 레이아웃)
- [x] 채팅 전송 후 모바일 키보드 깜빡임 완화 (조건부 focus, 중복 스크롤/reload 제거)
- [x] 채팅 Realtime 구독 필터 수정 및 publication 등록 migration 추가
- [x] 프로필 대표 링크 기능 구현 (`profile_links` 다중 확장 구조, 프로필 편집/표시)
- [x] 계정 탈퇴/복구 RPC soft delete 정책 보강 (게시글/스토리/댓글/메시지 복구 가능)
- [x] 국민대 Google 로그인/가입, 비밀번호 재설정, 친구 한정 실명 표시 구현
- [x] Supabase migration 로컬 파일 정리 (supabase/migrations/ 생성, 누락 4개 수동 추가)
- [x] **users 테이블 RLS 활성화 + 민감 컬럼 보호 트리거** (자가 admin 승격 방지, 개인정보 노출 차단)
- [x] **보안 패치** — anon 함수 실행 차단, Storage 파일 목록 노출 차단, 함수 search_path 고정
- [x] **앱 채팅 키보드/스크롤 구조 재정렬** — `react-native-keyboard-controller` 1.22 공식 채팅 구조에 맞춰 bottom safe-area, `KeyboardChatScrollView`, `KeyboardStickyView`, 입력창 증가분 패딩을 단일 기준으로 정리하고 전송 시 수동 스크롤을 제거
- [x] **앱 채팅방 이탈 시 키보드 잔류 수정** — 채팅방 뒤로가기/화면 이동/focus cleanup에서 `KeyboardController.dismiss()`를 호출해 iOS에서 다음 화면 위에 키보드가 남는 경로를 보강
- [x] **Google OAuth 연동** — 국민대 Google Workspace, 이름+학과 자동 파싱 저장 (프로필 사진 제외)
- [x] **실명 공개 범위** — 친구(accepted)이거나 본인일 때만 프로필에서 실명 노출
- [x] **Supabase Auth 설정** — 최소 비밀번호 8자, Letters+digits 조합 강제
- [x] **Google OAuth 실사용 테스트** — 실명+학과 자동 파싱 정상 확인 (Vercel)
- [x] **이메일 인증 정책 확정** — `@kookmin.ac.kr` 도메인 자체가 인증, 이메일 인증 OFF 유지
- [x] **팔로우 기능 설계 확정** — 단방향, 다른 학교는 팔로우만, 피드/스토리 크루→팔로우→전교생 순서
- [x] **`follows` 테이블 생성** — RLS, 인덱스, 제약 포함
- [x] **내 활동 1차 구현** — `/settings/activity`, 스토리 보관함, 스토리 날짜/조회자/좋아요 표시, 저장한 게시물, 게시물 저장/취소
- [x] **닉네임 정책 보강** — 이메일 앞부분 노출 방지, 온보딩 직접 입력, 활성 유저 기준 중복 방지 인덱스
- [x] **내 활동 2차 구현** — 좋아요한 게시물, 댓글 단 게시물, 즐겨찾기 계정 연결
- [x] **내 활동 탭별 lazy load 리팩토링** — 최초 진입은 스토리만 조회, 저장됨/좋아요/댓글은 백그라운드 prefetch
- [x] **차단 기능 1차 구현** — 게시물 차단 액션, 친구 관계 삭제, 피드/검색/프로필/게시물 상세 차단 필터링
- [x] **차단 기능 2차 구현** — 채팅 차단 반영, 차단 목록/해제 페이지, 채팅방 차단 버튼
- [x] **게시물/스토리 공개범위 정리** — `VisibilityPicker` 공통 컴포넌트, RLS를 `user_connections` 크루 기준으로 교체
- [x] **모바일 피드 이미지 폭 보정** — 홈 피드 게시물 이미지를 모바일에서 좌우 여백 없이 표시
- [x] **작성 화면 fullscreen 전환** — `/story/create`, `/write`에서 모바일 Header/BottomTabBar 제거 및 스토리 작성 화면 높이 보정
- [x] **게시물 비율 선택 1단계 기반 작업** — `posts.aspect_ratio` migration/API 타입/저장·조회 기본 경로 추가
- [x] **게시물 비율 선택 2단계 작성 화면 UI** — `/write` 비율 선택, 큰 미리보기, 썸네일 전환 확인, 저장 연결
- [x] **`/write` 데스크톱/모바일 레이아웃 재분리** — 데스크톱 사이드바 유지, 모바일 하단 탭바만 숨김
- [x] **게시물 비율 선택 3단계 표시 적용** — 피드/상세 이미지 프레임 고정, 다중 이미지 현재 위치 배지 추가
- [x] **게시물 다중 이미지 위치 배지 안정화** — 모바일 스와이프 중 숫자가 흔들리지 않도록 스크롤 종료 후 인덱스 갱신
- [x] **게시물 작성 UX 정리** — `/write` 데스크톱 폭 분리, 미리보기 최대 폭 제한, 썸네일/여백 보정
- [x] **Supabase migration 정리 현황 문서화** — 원격/로컬 이력 불일치 대조, repair 보류, `docs/MIGRATION_SYNC.md` 추가
- [x] **스토리 조회자 시트 UX 수정** — 공용 Avatar 적용, 조회자 클릭 시 프로필 이동 연결
- [x] **모바일 게시물 상세 댓글 UX 개선** — 모바일 상세 모달 fullscreen 보정, 댓글 바텀시트 연결
- [x] **모바일 게시물 상세 본문 UX 개선** — 긴 본문 3줄 접기/더보기 처리, 내부 스크롤 보정
- [x] **모바일 댓글 바텀시트 키보드 대응 보정** — 실기기에서 키보드 표시 시 헤더가 밀리는 문제 완화, visualViewport 기준 배치
- [x] **답글 멘션 링크 및 입력 포커스 보정** — 답글 자동 `@닉네임`만 프로필 링크 처리, 답글 클릭 시 입력창 포커스
- [x] **이미지 업로드 압축 (성능 Phase 1)** — `browser-image-compression`로 게시물/스토리/아바타 업로드 전 압축, GIF/실패 시 원본 fallback. 표시 최적화(next/image)는 Phase 2(Codex)로 분리
- [x] **이미지 표시 최적화 (성능 Phase 2)** — Supabase Storage remote pattern 등록, 피드/상세/탐색/프로필/내 활동/스토리 주요 이미지를 `next/image`로 전환
- [x] **전역 요청 중복 제거 1차 최적화** — `AppSessionProvider`로 현재 유저/알림/채팅 배지 조회와 Realtime 구독을 단일화, `Header`/`NavItems` 중복 fetch 제거, 차단 목록 30초 TTL 캐시 적용
- [x] **페이지 재방문 체감 속도 개선 1차** — 홈 피드/탐색 그리드/프로필 페이지 메모리 캐시로 라우트 복귀 시 즉시 표시
- [x] **홈 피드/프로필 첫 렌더 우선순위 조정** — 홈 피드는 해시태그/좋아요/저장 상태를 후순위로 미루고, 프로필은 헤더 우선 표시 후 그리드/관계 상태를 채움
- [x] **Expo 앱 1:1 DM(채팅) 구현** — 웹 채팅 API/Realtime 훅 포팅, `/messages` 목록/채팅방 라우트, 낙관적 전송, pending 수락, 홈 메시지 뱃지, 상대 프로필 메시지 진입 연결
- [x] **Expo 앱 내 활동 화면 구현** — 웹 `/settings/activity` 로직 포팅, 스토리/저장됨/좋아요/댓글/즐겨찾기 5탭 lazy load, 스토리 미리보기+조회자, 게시물/프로필 이동, 프로필 진입 버튼, 4번째 탭 라벨 `"탐색"` 정정
- [x] **Expo 앱 프로필 편집/차단한 계정 화면 구현** — 웹 프로필 편집/차단 목록 로직 포팅, `/profile/edit`·`/blocked` 라우트, 설정 Row 활성화, 사진/닉네임/소개/대표 링크 저장과 차단 해제 연결
- [x] **모바일 피드 댓글 진입 단축** — 모바일 홈 피드 댓글 버튼은 게시물 상세 이동 대신 `CommentSheet` 바텀시트를 즉시 열고, 데스크톱은 상세 이동 유지
- [x] **KREW 기반 1차 디자인 적용** — 연보라 배경/단색 보라 포인트/KREW 워드마크, KREW 스타일 하단 탭·헤더·사이드바, 카드형 모멘트바, 둥근 홈 피드카드 1차 적용
- [x] **KREW 기반 2차 디자인 적용 (검색/탐색)** — 공통 KREW 페이지/카드/섹션 컴포넌트를 추가하고 검색 화면 카드화, 탐색 2열 매스너리와 좋아요 pill 적용
- [x] **탐색 업로드 비율 반영** — 탐색 게시물 조회에 `aspect_ratio`를 포함하고, 2열 매스너리 타일을 업로드 시 선택한 게시물 비율로 렌더링하되 가로 게시물은 탐색 밀도 확보를 위해 정사각형으로 표시
- [x] **게시물 작성 첫 이미지 비율 자동 감지** — 새 게시물 작성 시 첫 이미지 실제 크기로 정사각형/세로/가로 비율을 자동 선택하고, 사용자가 수동으로 다시 변경 가능하도록 유지
- [x] **KREW 기반 3차 디자인 적용 (프로필/게시물 상세/댓글)** — 프로필 모바일 닉네임 헤더/카드형 헤더/그리드/액션 버튼, 게시물 상세/모달, 댓글 목록/댓글 바텀시트를 KREW 토큰 기준으로 재스타일링
- [x] **KREW 기반 4차 디자인 적용 (메시지/채팅/알림)** — 메시지 목록/채팅방/알림 모바일 페이지와 데스크톱 알림 패널을 KREW 배경·카드·보라 포인트 기준으로 재스타일링
- [x] **KREW 기반 5차 디자인 적용 (게시물 작성)** — `/write` 작성 화면, 미디어 선택, 비율 선택, 공개 범위, 본문/해시태그 입력을 KREW 토큰 기준으로 재스타일링
- [x] **KREW 기반 6차 디자인 적용 (프로필 편집/크루 관리)** — `/profile/edit`, `/profile/connections`를 KREW 카드형 폼/리스트/segmented tab 기준으로 재스타일링
- [x] **KREW 기반 7차 디자인 적용 (설정)** — `/settings`를 KREW 카드형 섹션/리스트/액션 버튼 기준으로 재스타일링
- [x] **KREW 기반 8차 디자인 적용 (내 활동/차단 목록)** — `/settings/activity`, `/settings/blocked`의 헤더/탭/그리드/리스트/빈상태를 KREW 배경·카드·보라 포인트 기준으로 재스타일링
- [x] **KREW 기반 9차 디자인 적용 (인증/온보딩)** — 로그인/회원가입/비밀번호 재설정/온보딩을 KREW 로고·연보라 배경·카드형 폼·보라 CTA 기준으로 재스타일링
- [x] **계정 전환 stale 캐시/세션 정리** — auth 상태 변경 시 홈 피드/탐색/프로필 메모리 캐시를 비우고 AppSessionProvider 현재 유저/뱃지 상태를 재동기화
- [x] **모바일 프로필 중복 메뉴 정리** — 타인 프로필 모바일에서 상단 `...`만 보이도록 프로필 카드 내부 친구 옵션 버튼을 모바일 숨김 처리
- [x] **카운터 갱신 RPC 전환** — 게시물 좋아요/댓글 수, 댓글 좋아요 수, 스토리 조회수 갱신을 직접 UPDATE에서 `recount_*` RPC 호출로 교체
- [x] **users 민감 컬럼 직접 SELECT 제거** — 프로필/현재 유저 조회에서 `email`/`real_name`/`fcm_token` 직접 SELECT를 제거하고 세션 email + `get_user_real_name` RPC 사용
- [x] **피드/모바일 게시물 상세 카드 룩 통일** — 홈 피드와 모바일 게시물 상세를 좌우 풀폭(edge-to-edge), 이미지 풀블리드, 그림자 없는 구조로 맞추고 모바일 상세 댓글 입력은 기존 `CommentSheet` 진입으로 유지
- [x] **프로필 카드/그리드 통합 레이아웃 보정** — 모바일 프로필에서 프로필 정보와 게시물 그리드를 하나의 KREW 카드 표면으로 묶고, 그리드 간격/썸네일 라운드를 정리
- [x] **Expo 앱 전환 검증 시작** — `apps/mobile` Expo SDK 54 TypeScript 앱 생성, 앱용 Supabase client/AsyncStorage 세션 기반 준비, 이메일 로그인/세션 확인 화면 1차 구현
- [x] **Expo Go 실기기 로그인 확인** — Android Expo Go 54.0.8에서 앱 실행 및 이메일 로그인 성공 확인
- [x] **Expo 앱 구조 분리** — `App.tsx` 올인원 검증 화면을 `app`/`screens`/`components`/`features`/`lib` 구조로 분리
- [x] **Expo 홈 피드 1차 구현** — 앱용 `getFeed`/`getLikedPostIds`/`togglePostLike` API와 `FlatList` 홈 피드, 새로고침, 무한 스크롤, 좋아요 토글 연결
- [x] **Expo 홈 피드 KREW 레이아웃 1차 보정** — 웹 모바일 홈에 맞춰 KREW 헤더, 내 스토리 카드, 둥근 피드 카드, 하단 탭바, 다중 이미지 배지를 추가
- [x] **Expo 앱 아이콘 체계 정리 1차** — `react-native-svg` + `lucide-react-native` 설치, 홈/피드/하단탭의 텍스트·이모지 아이콘을 lucide 아이콘으로 교체
- [x] **Expo 피드 다중 이미지 캐러셀 구현** — 앱 피드 미디어를 horizontal paging 캐러셀로 분리하고 현재 이미지 인덱스 배지를 실제 스와이프와 동기화
- [x] **Expo 댓글 바텀시트 1차 연결** — 앱용 댓글 조회/작성 API와 RN Modal 기반 댓글 시트, 댓글 아이콘 진입, 작성 후 댓글 수 갱신 연결
- [x] **Expo 댓글 바텀시트 반복 로딩 버그 수정** — 댓글 수 갱신 콜백 참조를 고정해 시트 로딩 effect 재실행 루프 차단
- [x] **Expo 피드 이미지 로딩 개선** — `expo-image` 적용, 피드 캐러셀 이미지 캐시/transition/recycling key 설정
- [x] **Expo 댓글 시트 자동 키보드 표시 제거** — 댓글 시트 열림 시 입력창 자동 focus 제거, 입력창을 직접 누를 때만 키보드 표시
- [x] **Expo 앱 탭 전환 구조 1차 구현** — 로그인 후 앱 셸에서 홈/검색/작성/활동/프로필 탭 상태 관리, 하단 탭 실제 버튼화, placeholder 화면 연결
- [x] **Expo Router 전환** — 수동 `useState` 탭을 파일 기반 라우팅으로 교체. `app/_layout`(세션 게이트)·`app/(tabs)`·`app/login` 신설, 세션 로직 `src/lib/session.tsx` 이전, `BottomTabBar`를 라우터 탭 상태 기반으로 변경, 옛 엔트리/수동 탭 파일 제거. 실기기 로그인/탭전환/세션유지 확인 + tsc 통과
- [x] **앱 탐색 화면** — 같은 학교 public 인기순 2열 masonry, 웹 ExploreGrid 비율 규칙(세로→4:5/정사각·가로→1:1), 둥근 타일+좋아요 뱃지, 무한 스크롤 (실기기 검증)
- [x] **앱 내 프로필 화면** — 웹 page 레이아웃(KrewSurface 패널: 아바타+게시물/크루+실명/학과+bio/링크+3열 1:1 그리드), `getProfile(nickname?)` 닉네임 분기 지원, 재사용 컴포넌트(KrewSurface/PostThumbnailGrid/ProfileInfoPanel) 분리
- [x] **앱 상대방 프로필 진입** — `app/profile/[nickname]`(탭 바깥), 피드/댓글 작성자 탭 → 프로필 이동, `UserInline` 공용 컴포넌트 (보기 전용)
- [x] **앱 게시물 상세 화면 1차** — `getPost(postId)` + `app/post/[id]`(탭바 숨김), FeedPostCard/CommentsSheet 재사용, 탐색·프로필 그리드 탭 → 상세 이동. 저장/신고/차단/삭제·연속피드는 다음 단계
- [x] **앱 댓글 고도화** — 대댓글(@멘션/접기펼치기) + 댓글 좋아요(낙관적+롤백) + 본인 삭제. `deleteComment`/`toggleCommentLike`/`getLikedCommentIds` 추가(기존 테이블·RPC), `CommentRow` 분리. 스키마 변경 없음
- [x] **앱 상대 프로필 크루 액션** — 친구 신청/수락/거절/삭제 RPC, 즐겨찾기 토글, 상대 프로필 `...` 액션시트, 낙관적 업데이트+롤백, 크루 수 갱신. 메시지 버튼은 앱 채팅 라우트 전까지 제외
- [x] **앱 스토리 작성 미리보기 뷰어 위치 통일** — 작성 미리보기 미디어를 StoryPlayer와 같은 `useStableInsets`/상단 정렬/검정 배경 기준으로 맞춤
- [x] **앱 크루 관리 화면** — `app/profile/connections`(탭바 숨김), 내 크루/받은 요청/보낸 요청 3탭, 수락/거절/취소/삭제, 유저 프로필 이동, 내 프로필 크루 통계 진입점 연결
- [x] **앱 게시물 작성 화면(사진 업로드)** — 작성 탭에서 사진 다중 선택/압축 업로드, 본문, 비율 자동감지+수동 선택, 공개범위 선택, `posts`/`post_media` 저장, 게시 후 홈 이동
- [x] **앱 상대방 프로필 라우트 1차 연결** — `(tabs)` 바깥 `app/profile/[nickname].tsx` 추가, 기존 `ProfileScreen` 재사용, 세션/파라미터 리다이렉트 처리
- [x] **앱 피드/댓글 작성자 프로필 이동 연결** — 공용 `UserInline` 분리, 피드 카드/댓글 작성자 탭 시 `/profile/[nickname]` 이동 연결
- [x] **앱 댓글 바텀시트 높이 보정** — 댓글 시트를 화면 대부분을 덮는 94% 높이로 조정하고 리스트/로딩 영역을 남은 공간에 맞춤
- [x] **앱 댓글 바텀시트 닫기 UX 보정** — `닫기` 버튼 제거, 핸들/헤더 드래그 닫기 구조 적용
- [x] **앱 댓글 바텀시트 드래그/상태바 재보정** — 반투명 배경 복구, Animated 드래그 닫기, Android StatusBar 배경/투명 설정 명시
- [x] **앱 safe area 1차 보정** — 루트 SafeAreaProvider, 주요 화면 SafeAreaView 교체, 하단 탭/댓글 모달 inset 반영
- [x] **앱 댓글 시트 닫힘 중복 모션 제거** — Modal slide 애니메이션 제거, 내부 Animated 닫힘만 사용, 중복 닫힘 호출 방지
- [x] **앱 댓글 시트 하단 여백/닫힘 튐 보정** — 시트 하단 고정 복구, 입력창 safe-area padding 적용, 닫힘 종료 프레임 튐 제거
- [x] **앱 스토리 기능 구현 (스토리바/작성/뷰어/자체 카메라)** — 이미지 업로드 공용 헬퍼 분리(피드도 적용), 웹 stories 로직 포팅(`features/stories`), 홈 스토리바(내 스토리 합침+그룹 카드), 갤러리+`expo-camera` 촬영 작성, 9:16 뷰어(세로 cover/가로·정사각 contain+블러, 진행바/일시정지/좌우탭/좋아요/삭제/신고/조회자), 신고 API 포팅. tsc 통과·파일별 커밋. ⚠️ 뷰어 상태바 복귀 침범 이슈는 이슈트래커 기록(dev build 재검증)
- [x] **앱 게시물 내 스토리 리셰어** — 본인 게시물 공유 시트에서 스토리 작성 팔레트로 진입해 원작자·첫 미디어·캡션 링크 카드를 게시하고, 뷰어 카드 탭으로 게시물 상세에 이동. `stories.shared_post_id`/nullable `image_url`/CHECK와 본인 글 전용 검증 트리거를 원격 적용하고 삭제·접근 불가 원본 리셰어는 목록/보관함에서 제외
- [x] **스토리 작성 기관·승격 계정 전용화** — `stories` INSERT RLS가 기관(`official_accounts`) 또는 승격(`users.is_promoted`) 계정만 허용하고, 앱 스토리바·게시물 리셰어·직접 작성 라우트도 기존 배지 캐시 기반으로 동일하게 차단. 일반 학생의 기존 스토리는 24시간 만료까지 유지
- [x] **일반 학생 스토리 안내 + 홈 첫 진입 코치마크** — 스토리가 없는 일반 학생도 홈 헤더 아래 40px 빈 스토리 영역을 유지하고, 사용자별 첫 홈 진입 한 번만 스토리바·검색·작성·탐색·프로필을 순서대로 안내. Context ref 레지스트리와 Tabs 루트의 단일 absolute 오버레이를 사용하고, 타깃/호스트 `measureInWindow` 결과를 같은 상대 좌표로 변환해 네이티브 Modal 중복과 상태바 좌표 오차 없이 처리
- [ ] **다른 화면 코치마크 확장** — 홈 코치마크의 공용 레지스트리·오버레이를 재사용해 작성·프로필 등 설명이 필요한 화면을 실제 사용자 피드백에 따라 선별 안내
- [ ] **웹 게시물 스토리 리셰어 대응** — 웹 스토리 조회·작성·뷰어에 링크 카드를 연결한 뒤 루트 `src/types/database.types.ts`의 nullable `stories.image_url`/`shared_post_id`를 동기화. 현재 앱 타입과 앱 경로만 신규 DB 구조를 사용
- [x] **앱 알림 기능 구현 (목록/뱃지/읽음/이동)** — 웹 `notifications/api.ts` 포팅(`target` 객체로 반환), 알림 화면+`app/notifications`, 홈 헤더 벨+안읽음 뱃지, 포커스 시 안읽음 수 갱신. 본인 행동이 알림 actor로 뜨던 버그 수정(actor 추정에서 본인 제외). tsc 통과·파일별 커밋. ⚠️ 웹 동일 버그 + `notifications.actor_id` 도입은 이슈트래커
- [x] **앱 검색 기능 구현 (유저 검색 + 최근 검색)** — `search_users` RPC 포팅(차단 제외), 최근 검색 `localStorage`→`AsyncStorage` 교체, 검색 화면(디바운스/결과/최근검색/프로필 이동)+탭 연결, 탭 전환 시 입력 초기화. tsc 통과·파일별 커밋. (발견 탭 확장=추천크루/공식·동아리/급상승은 Phase 2)
- [x] **앱 추천 크루 캐러셀** — 같은 학교의 공통 크루·같은 학과 후보를 우선 배치하고 남는 자리를 화면 세션 seed 기반 랜덤 후보로 채우는 `get_friend_recommendations` RPC와 검색 빈 화면의 가로 카드 캐러셀을 연결. accepted/pending·차단·기관/승격 계정 관계 제외, 카드 즉시 신청/세션 숨기기, 계정 배지·프로필 이동 지원. 카드 폭 148은 유지하고 높이·아바타·패딩·신청 버튼을 축소해 2~2.5장 노출과 오탭 방지를 보정. 학과 공개 opt-in 보호를 위해 같은 과 이유는 숨기고 공통 크루 수만 표시
- [x] **기관·승격 계정 신규 크루 관계 차단** — 기관/승격 프로필에서 신청·수락·삭제 등 크루 액션을 숨기고, `send_friend_request`·`accept_friend_request`가 호출자와 상대 양쪽의 `official_accounts`/`is_promoted`를 검사해 직접 RPC 우회도 거부
- [ ] **기관·승격 계정 기존 크루 데이터 정리** — 2026-08-11 원격 검사에서 확인된 accepted 1건의 계정/관계 정책을 확인한 뒤 제거하고 크루 수 집계를 재검증
- [~] **기관·승격 계정 팔로우 도입** — Phase 1 완료: 대상 제한 RLS, 팔로우 API, 프로필 팔로우/팔로잉 버튼, 팔로워 수와 낙관적 토글·롤백 연결. 남은 것: 팔로우 계정 콘텐츠 피드 밴드, 팔로워/팔로잉 목록, 팔로우 알림
- [~] **크리에이터 승격 신청 프로세스** — DB 신청·관리자 성적표·승인/거절·알림과 웹 관리자 목록·심사 화면 완료. 남은 작업은 앱 설정의 자격 안내·원탭 신청·알림 목적지 연결
- [~] **승격 계정 인사이트 확장 (A~E)** — 일반 계정은 개요만, 기관·승격 계정은 개요/콘텐츠 탭을 노출한다. v1 대시보드와 A 콘텐츠 성과를 연결했고 수집 기반이 필요한 후속 지표는 계속 확장한다.
  - [x] **프로필 진입점** — 승격·기관 계정의 내 프로필에서 프로필 편집 옆 인사이트 버튼으로 대시보드에 바로 진입. 배지 데이터 미로드·실패 시 숨기는 fail-closed 적용
  - [x] **A. 콘텐츠 성과** — 게시물별 좋아요·댓글·저장·공유를 전체 누적/게시물 행으로 제공하고 인기순·최신순 정렬을 지원한다. 행 탭은 게시물 인사이트 상세로 이동해 썸네일·작성일·조회·도달·상호작용을 단건으로 표시한다. 드롭다운 기간 선택, 기간별 조회·참여·도달 선택 타일, 단일 추이 차트, 유형별 조회 막대와 기관·승격 게이트까지 완료
  - [x] **인사이트 정확성 배치 A** — 개요·게시물 상세 도달을 `post_impressions` 고유 노출 계정으로 통일하고, 기간 변경·탭 비활성화 시 이전 비동기 응답을 무효화하며 집계 RPC 오류를 0으로 숨기지 않도록 수정
  - [x] **인사이트 용어 계약 정리** — 조회·도달·상호작용·영상 시청의 집계 기준과 화면별 범위를 코드에 명시하고 프로필 인사이트 진입 prop을 노출 의미로 정정
  - [ ] **B. 팔로워 인구통계** — 학교·학과·성별 등 팔로워 분포. 팔로우 도입 이후
  - [ ] **C. 게시물 노출 수집** — `post_impression` 수집 기준과 저장 구조를 확정한 뒤 콘텐츠 노출·전환 지표에 연결
  - [x] **D. 영상 시청 기록·상세 UI** — 릴스 활성 재생 세션을 `reel_watch_events`에 멱등 기록하고 완주율·평균 반복재생·평균 시청 깊이·평균 이탈 지점을 집계한다. 95% 완주, 85%→15% 루프 판정, 1초 미만 제외, 비활성/언마운트/백그라운드 종료까지 연결했다. 게시물 상세 인사이트에서 영상 시청 요약과 `max_pct` 5% 버킷 기반 유지율 곡선을 실제 초 축으로 표시한다. `watched_ms` 저장과 코호트별 유지율은 후속
  - [ ] **E. 학교 순위** — 같은 학교 내 콘텐츠/계정 순위

## 진행 중인 작업
- [x] **탐색(Explore) 탭 1차 (임시 그리드)** — 하단 탭 카테고리→탐색, 같은 학교 인기순 썸네일 그리드, 무한 스크롤. 본 기능(타 학교/승격/주간바이럴)은 다학교 확장 시
- [x] **스토리 영상 지원 DB 준비** — `stories`에 `type`/`thumbnail_url`/`duration` 추가(`post_media`와 동일), CHECK 제약, 라이브 적용, 웹/앱 타입·DATABASE.md 동기화. 게시물(`post_media`)은 이미 영상 컬럼 보유. **클라이언트(녹화/재생/압축/썸네일)는 별도 작업**
- [x] **앱 보안 검토 + 이미지 버킷 용량/형식 제한** — 검토 후 avatars(5MB)·post-images/story-images(10MB) 용량 + image/jpeg·png·webp 형식 제한 적용. 후속: #1 토큰 SecureStore(앱코드) 완료, #2 크루공개/DM private+signed URL(정책결정), #4 외부링크 https 검증
- [x] **앱 Google 로그인 + 온보딩 라우팅 가드** — `@react-native-google-signin/google-signin` 네이티브 로그인 추가, 로그인 화면 Google 버튼, 온보딩 화면, 세션 기반 온보딩 가드 연결. dev build 재빌드 후 실기기 확인 필요
- [x] **앱 iOS 구글 로그인 연결 (2026-07-30)** — 구글 콘솔 iOS OAuth 클라이언트 생성(웹/안드와 동일 프로젝트 `899969293498`, Bundle `com.univer.app`), `app.json` `iosUrlScheme` + `googleSignIn.ts` `iosClientId` + `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` 추가, iOS dev build 재빌드. nonce 에러는 Supabase Google provider "Skip nonce check" ON으로 해결(커스텀 nonce 유료). iOS 실기기 로그인 성공. 상세는 WORKLOG 2026-07-30
- [x] **앱 푸시 알림 클라이언트 구현** — `expo-notifications`/FCM 설정 연결, 로그인+온보딩 완료 후 Expo push token 발급→`users.fcm_token` 저장, 포그라운드 표시와 탭 라우팅 연결. 서버 전송은 Phase 2
- [x] **푸시 Phase 2 (서버 전송)** — pg_net 트리거로 댓글(post_comment)/대댓글(comment_reply)/DM(messages) Expo Push 전송. 대댓글 알림 신규(트리거+`notifications_type_check` 제약 추가 — 답글 INSERT 롤백 버그 수정), DM 탭 라우팅 클라 추가. 실기기 검증 완료. 남음: 인앱 comment_reply 문구/actor 처리, 좋아요·친구 푸시(의도적 보류)
- [x] **앱 게시물 메뉴 액션 연결** — `FeedPostCard` 저장 아이콘/`...` 메뉴를 저장·차단·신고 기능에 연결, 피드/상세 저장 상태 로드, 차단 후 피드 낙관적 제거, 신고 피드백 처리
- [x] **앱 채팅방 `...` 메뉴 연결** — 웹 채팅방과 동일하게 상대 차단 액션시트/확인 다이얼로그/차단 후 메시지 목록 이동 흐름 연결
- [x] **앱 게시물 본인 글 삭제 메뉴 연결** — `deletePost` soft delete API 추가, 본인 글 `...` 메뉴 삭제/확인 다이얼로그 연결, 홈 피드 낙관적 제거와 상세 뒤로가기 처리
- [x] **앱 스토리 영상 1단계 API plumbing** — `story-videos` 버킷 상수, 영상 raw 업로드(`uploadStoryVideo`), 영상 스토리 insert(`createVideoStory`) 추가. UI/재생/촬영은 다음 단계
- [x] **앱 영상 업로드 OOM 수정** — 큰 영상은 JS `ArrayBuffer`로 읽지 않고 `expo-file-system/legacy.uploadAsync` native binary upload 공용 유틸로 Supabase Storage에 직접 업로드
- [~] **앱 영상 업로드 전 압축 (폐기)** — `react-native-compressor`가 이 스택(RN 0.81 + New Architecture + 삼성 MediaCodec)에서 `Invalid to call at Released state`로 매번 실패. 미리보기 코덱 점유 가설도 검증 실패(언마운트 후에도 동일). 클라 압축 폐기, `compressVideoForUpload` 원본 통과로 정리. 실제 압축은 아래 Cloudflare Stream(출시 준비)로 이관
- [x] **앱 스토리 배경색 선택** — 사진/영상 스토리 프레임을 단색 배경 + contain 레터박스로 통일하고, 작성 팔레트 선택값을 `stories.background_color`로 저장/뷰어 반영
- [x] **앱 스토리 작성 미리보기 풀스크린 UI** — 카드형 미리보기를 풀스크린 오버레이로 전환하고, 무채색 배경색 바텀시트/하단 공개범위+공유 버튼 배치
- [x] **앱 게시물 작성 영상 1개 선택 추가** — 작성 화면에서 사진 여러 장 OR 영상 1개 배타 선택, 영상 썸네일/업로드/createPost(video) 제출 분기 연결
- [x] **Cloudflare Stream 영상 업로드/트랜스코딩 1차 전환** — 앱 영상 업로드를 Supabase Storage 원본 업로드에서 Cloudflare Stream direct upload로 전환. `post_media`/`stories`에 provider/asset/status 저장, Edge Function `stream-upload-url`/`stream-webhook` 배포, 처리 중 썸네일 UI 연결

### 보안 후속 과제 (영상 들어가기 전 점검)
- [x] **#1 인증 토큰 SecureStore 전환** — `supabase.ts` 저장소를 AsyncStorage→expo-secure-store(2KB 청크 어댑터)로 교체. 기존 AsyncStorage 세션 1회 마이그레이션 포함. dev build 재빌드 후 실기기 확인 필요
- [ ] **#2 민감 콘텐츠 접근제어** — 크루공개/DM 첨부/비공개 영상은 private 버킷 + signed URL(TTL 재사용). MVP 정책 결정 선행
- [ ] **#4 프로필 외부 링크 검증** — `ProfileInfoPanel` `Linking.openURL`을 https(s)만 허용

## 다음 작업 (순서대로)

1. Expo 앱 전환 vertical slice (라우터 전환 완료, 다음은 화면 연결)
   - 인증 배지 후속: 피드 캡션·스토리 헤더·알림 행 등 남은 유저 표시 경로로 확대
   - WSL2 + 실기기 실행은 `cd apps/mobile && npx expo start --tunnel` (LAN 모드면 번들 다운로드 실패)
   - Google 로그인 dev build 재검증(Web client ID env, 구글 시트 취소/성공, 신규 가입자 온보딩 이동, 완료 후 홈 이동)
   - 푸시 클라이언트 dev build 재검증(권한 요청, `users.fcm_token` 저장, 포그라운드 표시, payload 탭 시 post/story/profile 이동)
   - 스토리 영상 2단계(영상 선택/녹화, 썸네일 생성, `expo-video` 재생, 실기기/EAS 빌드 검증)
   - 작성 탭 실기기 확인(사진 여러 장 선택/삭제, 비율·공개범위 선택, 사진+본문/본문만 게시, 홈 피드 반영)
   - 상대 프로필 크루 액션 실기기 확인(친구 신청/취소, 수락/거절, 친구 삭제, 즐겨찾기 토글, 크루 수 갱신)
   - 크루 관리 화면 실기기 확인(내 프로필 크루 통계 → `/profile/connections`, 받은 요청 수락/거절, 보낸 요청 취소, 내 크루 삭제, 유저 프로필 이동)
   - 설정 화면 실기기 확인(프로필 편집 사진/닉네임/소개/링크 저장, 차단한 계정 목록/해제)
   - 검색/활동(탐색)/프로필 placeholder를 실제 조회 화면으로 연결 (`getProfile`/`getProfilePosts`/`getExplorePosts` 재사용)
   - 남은 세부 화면의 safe area/키보드/상태바 겹침 실기기 확인
   - `react-dom` 19.1.0 정렬(`npx expo install react-dom`) — 선택
   - 스토리 카드 크기/간격을 웹 모바일 홈 기준으로 재조정
   - 댓글 바텀시트 키보드/답글 포커스 실기기 재확인
   - **(앱 디자인 패스 보류 — 화면 다 모인 뒤 일괄 적용)** `docs/design/profile-explore-preview.html` 기준으로 맞출 것:
     - 프로필: 아바타 좌측 + 게시물/크루 통계 가로 배치 + 이름/학과/소개 + 편집/설정 버튼, 그리드를 흰 패널(KrewSurface)로 감쌈 (현재 앱은 아바타 가운데+로그아웃 버튼+크루수 없음, 명세와 다름)
     - 탐색: 타일 비율 staggered(4:5/1:1 혼합) 적용 (현재 4:5 통일)
2. KREW 기반 디자인 모바일 확인
   - 홈 피드 카드와 모바일 게시물 상세가 좌우 풀폭/풀블리드 이미지/그림자 없는 표면으로 보이는지 확인
   - 프로필 카드/게시물 그리드 통합 레이아웃이 모바일에서 과하게 좁거나 답답하지 않은지 확인
   - 카드형 모멘트바, 하단 탭 중앙 `+`, KREW 워드마크가 실기기에서 과하게 크거나 겹치지 않는지 확인
   - 검색/탐색/프로필/게시물 상세/메시지/채팅방/알림에서 기존 기능이 유지되는지 확인
3. KREW 디자인 다음 적용 범위
   - 후보: 공용 다이얼로그/액션시트 잔여 톤 정리, 스토리 조회자 시트 디테일 정리
4. 전역 요청 중복 제거 1차 최적화 배포 후 모바일 체감 확인
   - 홈/탐색/프로필/메시지 이동 시 Network 요청 수와 체감 지연 비교
   - 알림/채팅 배지 갱신, 프로필 탭 이동, 차단 후 피드/검색 반영, 계정 전환 후 stale 캐시 제거 확인
5. 페이지 재방문 캐시 배포 후 모바일 체감 확인
   - 홈/탐색/프로필에서 상세/다른 탭 이동 후 돌아왔을 때 스켈레톤 없이 즉시 표시되는지 확인
   - 좋아요/저장/차단/친구 상태 변경 후 캐시 화면이 어긋나지 않는지 확인
6. 홈 피드/프로필 첫 렌더 우선순위 조정 배포 후 체감 확인
   - 홈 피드 카드가 좋아요/저장 상태보다 먼저 표시되는지 확인
   - 프로필 헤더가 먼저 표시되고 이후 게시물/관계 상태가 자연스럽게 채워지는지 확인
7. 모바일 피드 댓글 진입 단축 배포 후 확인
   - 모바일 홈 피드 댓글 버튼이 바텀시트를 즉시 여는지 확인
   - 데스크톱 홈 피드는 기존처럼 게시물 상세/모달로 이동하는지 확인
   - 댓글 작성/삭제 후 피드 댓글 수가 정상 갱신되는지 확인
8. 이미지 표시 최적화 배포 후 모바일 체감 확인
   - 홈 피드, 탐색 그리드, 프로필 그리드, 내 활동 그리드, 스토리 뷰어 이미지 정상 표시/속도 확인
9. Supabase migration 원문 확보 및 로컬 파일 보강
   - `docs/MIGRATION_SYNC.md` 기준
   - `migration repair`는 원문/전략 확정 전 보류
10. 모바일 댓글 바텀시트 키보드/답글 포커스 보정 후 실제 기기 재확인
   - Android Chrome / iOS Safari 기준 댓글 입력창 포커스, 답글 자동 멘션 링크, 헤더 유지, 배경 스크롤 잠금 확인
11. 내 활동 보관함 고도화
   - 스토리 보관함 월별 그룹/캘린더 뷰
12. Resend 연동 (비밀번호 재설정용, 이메일 인증은 OFF 유지)
13. 계정 탈퇴/복구 실사용 테스트
14. **영상 서버 트랜스코딩 (Cloudflare Stream) — 파이프라인 완료(실기기 검증됨), 고도화 남음**
   - 완료: Cloudflare direct upload URL Edge Function, 앱 업로드 경로(XHR FormData), `post_media`/`stories` provider/asset/status 저장, 처리 중 UI, HLS URL 저장
   - 완료: **Cloudflare webhook 등록(`PUT stream/webhook`) + 실기기 end-to-end 검증** — 업로드→인코딩(~46초)→webhook 자동 호출→DB `processing→ready` 자동 전환→HLS 재생. 5단계 적응형(240p~1080p/60fps) 확인
   - 완료: 노출된 Stream 토큰 rotate + Supabase secret 교체 (교체 후 업로드 정상 확인)
   - 완료: 앱에서 `processing→ready` 자동 갱신(홈 피드 4초 폴링) + 완료 토스트 + "업로드 중" 라벨(사진/글/영상 통일)
   - 완료: 영상 길이(60초)/용량(250MB) 사전 제한(pickVideo 체크, 에러는 영상 선택 카드 밑 표시) + 서버 `maxDurationSeconds` 65초 이중 안전망
   - 완료: **릴스 신고/차단/삭제 메뉴**(`ReelItem` `...` → ActionSheet/ConfirmDialog 재사용, `useReels`에 reportPost/blockAuthor/removePost) — 릴스에 UGC 신고가 없어 심사 리스크였던 것 해소. tsc 통과, 실기기 확인 남음
   - 완료: **`stream-status` Edge Function**(2026-07-03) — webhook 놓쳐도 폴링이 Cloudflare 직접 조회→DB 갱신으로 영상 ready 자가복구. `getVideoStatuses`가 DB 읽기→함수 호출로 전환. 배포·tsc 통과(커밋 `cba8bc8`), 실기기 런타임 확인 남음
   - 완료: **옛날 Supabase Storage 영상 정리**(2026-07-03) — Cloudflare 전환 전 레거시 영상 9개가 loop 재생으로 반복 다운로드돼 cached egress 38GB 폭발한 사고. 게시물/스토리 soft delete + `.mp4` 실삭제 + `post-videos`/`story-videos` 버킷 private 전환
   - 남음: ① webhook 서명(`Webhook-Signature`) 검증 추가(위조 방지) ② 스토리 영상 길이 제한(현재 피드만 적용)
   - 완료: **릴스 processing·failed 영상 제외**(2026-08-27) — 릴스는 폴링 대신 본인 포함 ready 영상만 노출한다. 스토리는 기존처럼 작성자에게 처리 상태를 보여주고 타인에게는 ready 이후 노출하므로 의도된 동작이다.
   - 릴스 후속 갭: ⑥ 영상 글 0개일 때 빈 화면·startPostId 첫 페이지 밖이면 엉뚱한 영상 재생 ⑦ 전역 음소거(현재 영상마다 따로) ⑧ 더블탭 좋아요
   - 비용: Cloudflare Stream 저장 $5/1000분·전송 $1/1000분, 인코딩·업로드 무료 (MVP 규모 사실상 무시 가능)

### 검증 필요
- [ ] Google OAuth 신규 가입 시 이름+학과 자동 저장 확인
- [ ] 친구 아닌 유저 프로필에서 실명 안 뜨는지 확인
- [ ] 계정 탈퇴 후 피드/프로필/채팅 목록에서 탈퇴 유저 콘텐츠가 정상적으로 숨겨지는지 확인
- [ ] `restore_account()` 호출 시 30일 내 계정과 작성 콘텐츠가 정상 복구되는지 확인
- [ ] 모바일 댓글 바텀시트에서 키보드 표시 중 헤더/입력창/댓글 목록이 보이는 뷰포트 안에 유지되는지 확인
- [ ] 모바일 댓글 바텀시트에서 `답글 달기` 클릭 시 키보드가 즉시 열리고 자동 `@닉네임` 링크가 프로필로 이동하는지 확인
- [ ] 이미지 표시 최적화 후 피드/상세/탐색/프로필/내 활동/스토리 이미지가 Vercel 배포에서 정상 표시되는지 확인
- [ ] 전역 요청 중복 제거 후 알림/채팅 배지와 프로필 탭 링크가 모바일/데스크톱에서 정상 동작하는지 확인
- [ ] 페이지 재방문 캐시 적용 후 홈/탐색/프로필 복귀 화면이 즉시 표시되고 UI 상태가 어긋나지 않는지 확인
- [ ] 홈 피드/프로필 첫 렌더 우선순위 조정 후 부가 상태가 늦게 붙어도 UI가 어색하게 깜빡이지 않는지 확인
- [ ] 프로필 로딩 스켈레톤이 실제 헤더·풀와이드 카드·게시물 그리드와 같은 영역으로 자연스럽게 전환되는지 확인
- [ ] 다크모드 Phase 3 채팅에서 메시지 목록·채팅방·말풍선·입력창이 라이트/다크 모두 선명하고 키보드 하단 safe area까지 자연스럽게 이어지는지 확인
- [ ] 장식용 아웃라인 제거 후 라이트/다크에서 카드·패널·버튼·칩·말풍선이 배경만으로 구분되고 입력 필드·구분선·스토리 링은 그대로 유지되는지 확인
- [ ] 다크모드 G2 검색·탐색·알림에서 화면 배경·카드·텍스트·로딩/빈 상태·알림 읽음 상태가 라이트/다크 모두 선명한지 확인
- [ ] 다크모드 G3 작성·게시물 상세에서 카드·입력·비율/공개범위 선택·미디어 미리보기·게시 버튼·상세 피드백이 라이트/다크 모두 선명한지 확인
- [ ] 다크모드 G4 설정·활동·인사이트·인증에서 카드·탭·그리드·그래프·입력·채운 버튼이 라이트/다크 모두 선명하고 fixed 미디어/Google 브랜드 색이 유지되는지 확인
- [ ] unip 워드마크가 홈·로그인·온보딩·탭 placeholder·환경 오류 화면에서 적절한 크기와 라이트/다크 색으로 표시되는지 확인
- [ ] 모바일 홈 피드 댓글 버튼이 바텀시트를 즉시 열고 댓글 수 갱신이 정상 반영되는지 확인
- [ ] KREW 기반 1차 디자인 적용 후 홈 모멘트바/피드카드/하단탭이 모바일에서 겹치지 않고 기존 기능이 정상 동작하는지 확인
- [ ] KREW 기반 2차 디자인 적용 후 검색 입력/최근 검색/검색 결과/탐색 매스너리/탐색 상세 진입이 정상 동작하는지 확인
- [ ] 새 게시물 작성에서 첫 이미지 실제 크기에 맞게 비율 선택이 자동 변경되고, 탐색 매스너리에서 세로/정사각형은 해당 비율, 가로는 정사각형으로 표시되는지 확인
- [ ] KREW 기반 3차 디자인 적용 후 프로필 편집/설정/크루 관리 이동, 프로필 게시물 상세 진입, 상세 댓글 작성/답글/삭제가 정상 동작하는지 확인
- [ ] KREW 기반 4차 디자인 적용 후 메시지 검색/대화 진입/요청 수락/메시지 전송/알림 읽음 처리와 알림 링크 이동이 정상 동작하는지 확인
- [ ] KREW 기반 5차 디자인 적용 후 새 게시물 작성/수정, 이미지 선택/삭제, 비율 자동 감지/수동 변경, 공개 범위/해시태그 저장이 정상 동작하는지 확인
- [ ] KREW 기반 6차 디자인 적용 후 프로필 사진 변경/저장, 닉네임 중복 확인, 대표 링크 저장, 크루 요청 수락/거절/삭제가 정상 동작하는지 확인
- [ ] KREW 기반 7차 디자인 적용 후 설정 메뉴 이동, 로그아웃, 계정 탈퇴 확인 다이얼로그가 정상 동작하는지 확인
- [ ] KREW 기반 8차 디자인 적용 후 내 활동 탭 전환/게시물 이동/스토리 미리보기/차단 해제가 정상 동작하는지 확인
- [ ] KREW 기반 9차 디자인 적용 후 Google 로그인, 이메일 로그인/회원가입, 비밀번호 재설정, 온보딩 저장이 정상 동작하는지 확인
- [ ] 같은 브라우저 탭에서 A계정 본인 프로필 확인 → 로그아웃 → B계정 로그인 → A 프로필 진입 시 B 관점의 버튼/실명 노출 상태로 표시되는지 확인
- [ ] 카운터 RPC 전환 후 게시물 좋아요/취소, 댓글 작성/삭제, 댓글 좋아요/취소, 스토리 조회 시 카운트가 정상 동기화되는지 확인
- [ ] users 민감 컬럼 직접 SELECT 제거 후 온보딩 실명/이메일 표시, 본인·크루 프로필 실명 표시, 비크루 실명 비노출이 정상 동작하는지 확인
- [ ] 앱 스토리: 작성(카메라 촬영/전후면/플래시/갤러리→미리보기→공유), 스토리바(내 스토리 합침/추가 배지/그룹 카드), 뷰어(9:16 표시, 진행바/좌우탭/일시정지, 좋아요, 본인 삭제·조회자, 타인 신고)가 실기기에서 정상 동작하는지 확인
- [ ] 앱 스토리 뷰어 상태바 복귀 침범 — dev build에서 재현 여부 및 화면별 시스템바 제어 방식 재검증
- [ ] 앱 알림: 벨 뱃지/목록 문구/탭 시 읽음+이동(게시물/스토리/프로필), 모두 읽음, 본인 행동이 actor로 안 뜨는지 실기기 확인
- [ ] 앱 검색: 닉네임 입력→결과→프로필 이동, 최근 검색 저장/삭제/모두지우기, 탭 전환 시 입력 초기화 실기기 확인 (확인 완료)
- [ ] 앱 채팅방: iOS 실기기에서 키보드 닫힘 하단 여백, 열림 시 최신 메시지 가림, 전송 직후 위치 튐, 닫힘 bounce, 키보드 열린 상태의 뒤로가기/스와이프 백 재확인

### 앱 전환 전 정리 체크리스트
- [ ] `features/chat/api.ts`의 `window.dispatchEvent` 제거 또는 hook 레이어로 이동
- [ ] `features/chat/hooks.ts`의 `window` 이벤트 사용을 웹 전용 refresh bus로 분리
- [ ] `features/auth/api.ts`의 `window.location.origin` 기반 redirect를 플랫폼별 callback URL 주입 방식으로 변경
- [ ] `features/search/history.ts`의 `localStorage` 접근을 웹 storage adapter로 분리
- [ ] 이미지 업로드 API의 `File` 의존성(`uploadPostImages`, `uploadStoryImage`, `uploadAvatar`)을 앱 업로드 입력 타입과 분리
- [ ] `features/*/api.ts`에서 DOM/Next Router 의존성이 새로 들어오지 않도록 점검
- [ ] Expo 전환 시 재사용할 핵심 타입 정리: Profile, FeedPost, StoryGroup, Message, Conversation

### 1단계 — SNS MVP
1. **인증** — 학교 이메일 가입, 구글/카카오, 온보딩 (학교/학과/닉네임)
   - 완료: 학교 이메일 도메인 검증, 이메일+비밀번호 로그인, 회원가입, 온보딩 저장, middleware 보호 재활성화
   - 완료: users 타입에 real_name 컬럼 반영
   - 완료: 회원가입 이메일 인증 리다이렉트(`/auth/callback`) 및 인증 메일 발송 완료 화면 적용
   - 완료: 계정 탈퇴 RPC 연동 및 설정 페이지 탈퇴 흐름 구현
   - 남음: 비밀번호 재설정 등 부가 흐름
2. **레이아웃** — 모바일/웹 공통 레이아웃, Header, BottomTabBar, SideBar
   - 완료: 메인 화면 껍데기 및 반응형 3단 레이아웃
   - 완료: 현재 경로 기반 사이드바/하단탭 활성 상태 처리
   - 완료: NavItems 클라이언트 컴포넌트 분리
   - 완료: Avatar 공용 컴포넌트 기반 프로필 아이콘 표시
   - 완료: 메인 피드 컬럼 폭 `max-w-[470px]` 복구
   - 완료: `/search` 유저 검색 페이지 및 최근 검색 UI 구현
3. **피드** — 게시물 CRUD, 사진 업로드, 무한 스크롤
   - 완료: 게시물 이미지 업로더 UI 추가
   - 완료: 게시물 작성 페이지 UI 및 저장 로직 추가
   - 완료: `getFeed`, `PostCard`, `FeedList`, 메인 피드 연결
   - 완료: UserInfo 공용 컴포넌트로 작성자 아바타+닉네임 링크 공통화
   - 완료: 피드 이미지 원본 비율 표시 (object-contain, 검정 배경)
   - 완료: 무한 스크롤
   - 완료: PostCard 이미지 슬라이드 화살표 버튼
   - 완료: 좋아요 기능 (DB 연결 + 낙관적 업데이트)
   - 완료: 댓글 / 대댓글 API 1차 구현
   - 완료: 댓글 바텀시트 UI 및 피드 연결
   - 완료: 댓글 좋아요 기능 (DB 연결 + 낙관적 업데이트)
   - 완료: 대댓글 1단계 중첩 UI 및 hard delete 전환
   - 완료: 게시물 수정 모드 (본문/해시태그 수정, 이미지 읽기 전용)
   - 완료: PostCard ... 버튼 메뉴 (본인: 수정/삭제/링크 복사 / 타인: 신고/차단/링크 복사)
   - 완료: 게시물 저장/저장취소 기능 및 저장 상태 표시
   - 완료: 게시물 삭제 soft delete 및 피드 즉시 제거
   - 완료: 토스트 메시지 컴포넌트 및 작성/수정/삭제/링크 복사 피드백 연결
   - 완료: 더보기 버튼 (텍스트 말줄임)
   - 완료: 게시물 상세 UI 컴포넌트 및 `/posts/[postId]` 직접 접근 페이지 구현
   - 완료: 웹 인터셉트 모달 및 프로필 썸네일 상세 이동 연결
   - 완료: 게시물 작성/수정 진입 경로 `/write` 추가 및 링크 변경
   - 완료: 게시물 미디어 테이블 `post_media` 전환 및 `PostMedia` 타입 적용
   - 완료: 게시물 상세 데스크톱 모달 이미지/댓글 입력 레이아웃 보정
   - 완료: 게시물 상세 댓글 좋아요/대댓글/답글달기 기능 복구
   - 완료: 게시물 상세 컴포넌트 파일 분리
   - 완료: 기존 `/posts/write` 라우트 삭제
   - 다음: 좋아요 목록 모달
4. **좋아요 / 댓글 / 대댓글**
5. **스토리** — 업로드, 뷰어, 24시간 자동 만료
   - 완료: `StoryBar` UI 추가, 더미 데이터 제거 완료
   - 완료: 스토리 작성 1차 구현 (사진 1장 업로드, stories 저장, 24시간 만료 설정)
   - 완료: 스토리바 실제 데이터 조회 연결 (같은 학교/만료 전/읽음 여부/본인 우선)
   - 완료: 내 스토리 생성 아이템 아바타 표시, 내 스토리 중복 제거, 진입/생성 클릭 분리
   - 완료: 스토리 뷰어 연결 (9:16 비율, 5초 자동 진행, 조회 기록, 본인 조회자 목록, 좋아요)
   - 완료: 스토리 삭제 soft delete 및 ... 메뉴 연결
   - 완료: stories SELECT RLS soft delete 호환 정책 수정
   - 완료: ConfirmDialog 공용 컴포넌트 및 스토리 삭제 확인 흐름 연결
   - 완료: 스토리바 정렬을 내 스토리 → 크루 → 같은 학교 순서로 개선
   - 완료: 스토리 뷰어에서 이전/다음 유저 스토리 이동 및 사이드 프리뷰 구현
   - 완료: 스토리 뷰어 3열 그리드 배치 및 화살표 위치 보정
   - 완료: 본인 게시물 내 스토리 리셰어 — fixed 흰 카드와 8px 흰 프레임의 각진 사진·영상 박스, 원본 비율 표시, 공유 영상 무음 반복 및 15초 세그먼트, 카드 탭 게시물 상세 이동
   - 다음: 스토리 UI/UX 개선
6. **프로필 페이지**
   - 완료: 프로필 조회/프로필 게시물/게시물 수 API 1차 구현
   - 완료: `/profile/[nickname]` 페이지 1차 구현 (프로필 영역, 게시물 3열 그리드, 로딩 스켈레톤)
   - 완료: `/profile/me` 현재 유저 프로필 alias 처리
   - 완료: 프로필 게시물 그리드 모바일 전체 폭 확장 및 탭 구분선 추가
   - 완료: 프로필 게시물 그리드 웹 폭 확장
   - 완료: 프로필 버튼에 현재 유저 프로필 사진 표시
   - 완료: 프로필 편집 페이지 구현 (프로필 사진/닉네임/소개 수정)
   - 완료: `features/profile/mutations.ts` 분리
   - 완료: `signOut()` 추가
   - 완료: 전체 button 태그 cursor-pointer 전역 적용
   - 완료: 설정 페이지 구현 (`/settings`, 로그아웃 포함)
   - 완료: 본인 프로필 설정 아이콘 추가
   - 완료: 프로필 편집 페이지 로그아웃 버튼 제거
   - 완료: 자체 헤더 화면용 `(sub)` route group 분리
   - 완료: 프로필 게시물 썸네일 상세 이동 연결
   - 완료: 타인 프로필 친구 신청/수락/거절/삭제 UI 및 RPC 연동
   - 완료: 프로필 대표 링크 저장/표시 구현 (`profile_links` 테이블 기반, 추후 다중 링크 확장 가능)
   - 완료: `profile_links` 원격 Supabase migration 적용 및 로컬 저장/표시 테스트
   - 완료: 관리자 전용 레이아웃, 대시보드, 신고 관리, 유저 관리 1차 구현
   - 다음: 관리자 액션 범위 확장
7. **유저 좋아요 + 친한친구**
   - 완료: `user_connections` 기반 친구 신청/수락/거절/삭제 흐름 적용
   - 완료: 크루 목록/받은 요청/보낸 요청 관리 페이지 구현
   - 다음: 친한친구 기능 설계 및 구현
8. **북마크 / 해시태그**
9. **알림**
   - 완료: `features/notifications/api.ts` 신규 생성 (`getNotifications`, `getUnreadCount`, `markAsRead`, `markAllAsRead`)
   - 완료: 웹 알림 패널 구현 (사이드바 벨 클릭, 전체 읽음, 읽음 상태, 썸네일, 이동 처리)
   - 완료: 모바일 알림 페이지 구현 (`/notifications`)
   - 완료: Header/NavItems 벨 아이콘 읽지 않은 알림 뱃지 표시
   - 완료: 알림 목록 actor 닉네임 역추적 표시 보정
   - 완료: 친구 신청/수락 알림 타입 반영
10. **차단 / 신고**
   - 완료: `reports` 테이블 기반 피드 게시물/스토리 신고 연결
   - 완료: 신고 전 확인 다이얼로그 및 성공/실패 피드백 연결
   - 완료: 게시물 `...` 메뉴 차단 액션 연결 및 차단 관계 유저 피드/검색/프로필/상세 조회 숨김
   - 완료: 채팅 차단 반영 (대화 목록 필터, 메시지 전송 차단, 채팅방 차단 버튼)
   - 완료: `/settings/blocked` 차단 목록/해제 페이지 추가
11. **공개범위**
   - 완료: `VisibilityPicker` 공통 컴포넌트 (전체공개/크루공개, 게시물·스토리 재사용)
   - 완료: posts/stories 작성 화면 공개범위 선택 연결
   - 완료: 크루공개 RLS를 `user_connections(status='accepted')` 기준으로 교체

### 2단계 — 채팅
- 방식: Supabase Realtime 기반 1:1 DM (외부 라이브러리 없이 자체 구현)
- DB: conversations (대화방), messages (메시지 + read_at 읽음처리)
- 완료:
  - `features/chat/api.ts` + `hooks.ts` (Realtime 구독 포함)
  - `components/chat/` (`ConversationItem`, `MessageBubble`, `MessageInput`)
  - `/messages` (대화 목록), `/messages/[conversationId]` (채팅방)
  - 프로필 페이지 "메시지 보내기" 버튼
  - 헤더/사이드바 메시지 아이콘 뱃지 (안읽은 메시지 수, Realtime 즉시 갱신)
  - 메시지 간격 축소, 시간 표시 hover 시 옆에 표시, 5분 구분선
  - NavItems Realtime 채널 중복 버그 수정
  - 메시지 낙관적 전송 처리 및 Realtime 중복 표시 방지
  - 전송 후 입력창 포커스 유지 및 하단 스크롤 보정
  - 채팅방을 `(fullscreen)` route group으로 분리하고 뒤로가기를 `/messages`로 고정
  - 메시지 구독 filter 복구 및 채팅 테이블 Realtime publication 등록
- MVP 제외: 사진 전송, 타이핑 인디케이터, 메시지 삭제/수정, 그룹채팅, 읽음 실시간 반영

### 3단계 — 커뮤니티
- 자유게시판, 베스트, 수업찾기 (카테고리 확정 후)

### 4단계 — 앱 전환
- Expo 세팅, 모노레포 구조 전환

## 2026-04-28 업데이트

### 방금 완료
- [x] 메인 피드 초기 연결 완료
- [x] 피드 카드 / 스토리바 / 웹 사이드바 1차 레이아웃 보정 완료
- [x] 웹 사이드바 sticky 고정 처리 완료
- [x] 인증/피드/스토리/Supabase 유틸 주석 정리 및 운영 문서 최신화 완료

### 남은 점검
1. 배포 전 이메일 인증/리다이렉트 설정 확인
2. 배포 후 스토리 만료 정리용 Cron Job 구성
3. Storage 업로드 정책 및 파일 크기/type 제한 점검
4. 핵심 플로우 실기기 테스트 후 치명 버그만 수정

## 주의사항
- 영상 방침 변경(2026-06-26): 스토리 영상은 MVP 포함 목표(DB 준비 완료), 피드 영상은 출시 후 fast-follow. 클라이언트 작업(녹화/재생/압축/썸네일) 미착수
- 디자인은 나중에, 지금은 구조/로직 우선
- 모든 컴포넌트 모바일 퍼스트 (375px)
- features/ 로직은 UI와 반드시 분리 (앱 전환 대비)
- Supabase 쿼리는 features/*/api.ts 에서만
